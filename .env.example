# Django
SECRET_KEY=seu-secret-key-muito-longo-e-aleatorio
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SETTINGS_MODULE=config.settings.development

# Database (local: localhost, docker full-stack: db)
DATABASE_URL=postgresql://wisecofre:password@localhost:5432/wisecofre

# Redis / Celery (local: localhost, docker full-stack: redis)
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1

# Email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=mailhog
EMAIL_PORT=1025
EMAIL_USE_TLS=False
DEFAULT_FROM_EMAIL=wisecofre@localhost

# Segurança
ENCRYPTION_KEY=fernet-key-gerado-com-Fernet.generate_key()
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=30
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# App
APP_BASE_URL=http://localhost:8000
APP_NAME=Wisecofre

# MinIO / S3 (local: localhost:9000, docker full-stack: minio:9000)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=wisecofre-access-key
MINIO_SECRET_KEY=wisecofre-secret-key-muito-longa
MINIO_BUCKET_NAME=wisecofre-files
MINIO_USE_HTTPS=False
MINIO_PRESIGNED_URL_EXPIRY_SECONDS=60
MINIO_UPLOAD_URL_EXPIRY_SECONDS=900
FILE_MAX_SIZE_BYTES=10485760
